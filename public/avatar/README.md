# Avatar Asset Intake

Place the supplied control-center avatar at:

```text
public/avatar/control-center-avatar.glb
```

Expected asset:
- GLB model file.
- Keep the filename stable so the dashboard can load it at `/avatar/control-center-avatar.glb`.

Fallback behavior:
- The home stage renders the GLB model directly.
