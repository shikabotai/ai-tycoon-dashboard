# Generated projection data

This directory is reserved for build-time generated projection payloads.

Phase 3 production hardening is moving toward exporting personal projection JSON here before deploy, so Cloudflare Pages Functions can serve projection data without reading local PunkRecords files at runtime.
